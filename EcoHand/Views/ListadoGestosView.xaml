<UserControl x:Class="EcoHand.Views.ListadoGestosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EcoHand.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cal ="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">




    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
   
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>

            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <!--<ListView Grid.Column="1" Grid.Row="1" x:Name="Listado">
            
        </ListView>-->


        <!-- Row 1 Col 1 -->

        <Button Style="{StaticResource RadioButton}" Grid.Column="1" Grid.Row="1"  Content="Nuevo" x:Name="LoadEditorDeGestos" />


        <ListBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" x:Name="Gestos" 
                 SelectedItem="{Binding SelectedGesto}">

            <ListBox.ItemTemplate>
               <DataTemplate>
                  <TextBlock   Text="{Binding Path=Nombre}"/>
               </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
        
        
        

        <ContentControl Grid.Column="3" Grid.Row="1" Grid.RowSpan="2" x:Name="ActiveItem" />

        <Button Style="{StaticResource RadioButton}" Grid.Row="3" x:Name="LoadEditarById" Grid.Column="3" MinWidth="120" MaxWidth="200" Margin="225,0,0,0" >Editar</Button>
        <Button Style="{StaticResource RadioButton}" Grid.Row="3" x:Name="EliminarGesto" Grid.Column="3" Margin="0,0,225,0"  MinWidth="120" MaxWidth="200" >Eliminar</Button>

    </Grid>
</UserControl>
