<UserControl x:Class="EcoHand.Views.ListadoGestosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EcoHand.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cal ="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">




    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>

            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <!--<ListView Grid.Column="1" Grid.Row="1" x:Name="Listado">
            
        </ListView>-->


        <!-- Row 1 Col 1 -->

        <Button  Grid.Column="1" Grid.Row="1"  Content="Nuevo" x:Name="LoadEditorDeGestos" />


        <ListView Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" x:Name="Gestos"

                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  IsSynchronizedWithCurrentItem="True"
                  SelectionMode="Single"
                 
                  >

            <ListView.ItemTemplate>
               
          

                <DataTemplate>
                    <DataTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            
                        </Trigger>
                    </DataTemplate.Triggers>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ListViewItem Grid.Column="0">
                            <TextBlock Text="{Binding Path=Nombre}"/>
                        </ListViewItem>
                        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Right"  Grid.Column="1">
                            <ListViewItem cal:Message.Attach="[Event MouseDoubleClick] = [Action LoadHandAsync]">
                              
                                <materialDesign:PackIcon  Kind="ShowOutline" Foreground="Green" />
                            </ListViewItem>
                            <ListViewItem  cal:Message.Attach="[Event MouseDoubleClick] = [Action LoadEditarById(SelectedItem.Id)]">
                                <materialDesign:PackIcon  Kind="EditOutline" Foreground="Blue"/>
                            </ListViewItem >
                            <ListViewItem cal:Message.Attach="[Event MouseDoubleClick] = [Action EliminarGesto]">
                                <materialDesign:PackIcon x:Name="EliminarGesto"  Kind="DeleteOutline" Foreground="Red"/>
                            </ListViewItem>
                        </StackPanel>

                    </Grid>


                </DataTemplate>



            </ListView.ItemTemplate>

        </ListView>

        <ContentControl Grid.Column="3" Grid.Row="1" Grid.RowSpan="2" x:Name="ActiveItem" />

    </Grid>
</UserControl>
